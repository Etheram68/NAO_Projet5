{% extends "layout.html.twig" %}

{% block title %}Carte - {{ parent() }}{% endblock %}

{% block body %}
    <header class="header-404 overlay full-404 relative v-center" id="home-page">
        <div class="absolute anlge-bg"></div>
        <div class="container">
            <div class="row v-center">
                <div class="col-xs-12 col-md-7 header-text">
                    <h2>LES OBSERVATIONS</h2>
                    <h3>sur la Carte</h3>
                </div>
            </div>
        </div>
        </div>
    </header>

    <section class="section-padding gray-bg" id="blog-page">
        <div class="container">
            <div class="row">
                <div class="col-xs-12 col-sm-4">
                            <form id="search-map-widget" method="post" style="width: 145%; top: 0px;">
                                <a id="action-map-search" href="#" class="light-2"><i class="material-icons">search</i></a>
                                <input type="text" id="search-bird" name="search-bird" autocomplete="off" placeholder="Rechercher par espèce"/>
                            </form>
                    <a href="{{ path('fiche.create') }}" class="btn-floating btn-large waves-effect waves-light" style="background-color: #FFDE00;"><i class="material-icons" style="color: #333;">add</i></a>
                </div>
            </div>
        </div>
    </section>

    <section class="section-padding-card gray-bg" id="blog-page">
        <div class="container">
            <div class="row">
                <div class="col-sm-12">
                    {% if bird|length > 0 %}
                    {% for bird in bird %}
                    {% endfor %}
                    {% else %}
                        <h2>Il n'y a pas encore de fiche</h2>
                    {% endif %}
                </div>
            </div>
        </div>
    </section>

{% endblock %}

{% block script %}

{% endblock %}