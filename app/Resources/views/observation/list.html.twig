{% if obslist|length > 0 %}
    {% for observation in obslist %}
        <div class="col s12 m6" >
            
        </div>
    {% endfor %}
{% else %}
    <div class="col s12" >
        Il n'y a pas d'observation correspondante aux critères de votre recherche.
    </div>
{% endif %}